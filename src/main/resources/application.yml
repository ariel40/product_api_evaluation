server:
  port: 8081
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true

logging.level.org.springframework.security: off
logging:
  level:
    org:
      springframework:
        security:
          web:
            FilterChainProxy: off
